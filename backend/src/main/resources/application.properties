# Use PORT environment variable if set, otherwise default to 9092 (changed from 9091)
server.port=${PORT:9092}

spring.application.name=portfolio-backend

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/portfolio_db
spring.datasource.username=root
spring.datasource.password=n3u3da!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# API Documentation
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# ------------------------------------------------------------------
# AI provider configuration (used by AiService)
# DO NOT commit real secrets to source control.
# Set these via environment variables (recommended):
#   - AI_API_KEY: Your Google Gemini API key
#   - AI_API_KEY_LOCATION: How to send the key (header or query)
#   - AI_API_KEY_HEADER: Header name when using header location (default: x-goog-api-key)
#   - AI_API_URL: The Gemini API endpoint
# Example for Google Gemini:
#   AI_API_KEY=your-key-here
#   AI_API_KEY_LOCATION=header
#   AI_API_KEY_HEADER=x-goog-api-key
#   AI_API_URL=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent

ai.api.key=${AI_API_KEY:}
ai.api.key-location=${AI_API_KEY_LOCATION:header}
ai.api.key-header=${AI_API_KEY_HEADER:x-goog-api-key}
ai.api.url=${AI_API_URL:https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent}
